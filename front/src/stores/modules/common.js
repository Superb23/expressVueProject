import { defineStore } from "pinia";

const useCommonStore = defineStore("common", {
  state: () => ({
    menuTheme: 1,
  }),
  getters: {

  },
  actions: {
    
  }
})

export default useCommonStore;